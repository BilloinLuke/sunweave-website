# 2026-07-12

## 买家聊天记录按 IP 存到 Gitee
- 新增 `api/chatlog.js`：用 Gitee Contents API 把每段对话按 IP 存成 `chat-logs/<ip>.csv`，同 IP 多次访问追加到同一文件。列：timestamp,ip,role,message。
- 零依赖，复用 `api/net.js` 的代理感知（本地走 CHAT_PROXY_URL，Vercel 直连）。
- `api/providers.js` 的 `handleChat(body, req)` 增加 `req` 参数取 IP，回复后 `chatlog.logTurn()` 持久化（带超时，失败不影响聊天）。`server.js` 与 `api/chat.js` 均已传 `req`。
- 新增环境变量：CHAT_LOG_ENABLED / GITEE_TOKEN / GITEE_LOG_REPO / GITEE_LOG_BRANCH / GITEE_LOG_PATH / CHAT_LOG_TIMEOUT。已写进 .env 与 VERCEL_DEPLOY.md。
- 防护：CSV 公式注入（买家输入 =/+/-/@ 开头加前缀 '）、同 IP 写入串行化+409 重试、超时放弃。
- 测试：用内存 mock 模拟 Gitee API 验证 per-IP 分文件/合并/失败仍记 user 行/注入防护，全部通过，已删除临时测试脚本。

## 待用户操作（我无法替其登录 Gitee/Vercel）
- 在 Gitee 生成有 repo 写权限的私人令牌，填入 GITEE_TOKEN（.env 本地 + Vercel 环境变量）。
- ⚠️ 隐私：聊天含买家 PII，日志仓库务必私有；默认 GITEE_LOG_REPO=luke888888/sunweave-kb（当前为公开仓库），建议改成专门私有仓库或把该仓库设为私有。
- 重启本地 `node server.js` 或 Vercel 重新部署后生效。

## 聊天日志仓库改为 shouji-kb
- 用户提议把聊天日志仓库从 `sunweave-kb`（公开，存知识库）改到 `luke888888/shouji-kb`。
- 已把 `.env` 与 VERCEL_DEPLOY.md 的 `GITEE_LOG_REPO` 改为 `luke888888/shouji-kb`（去掉了 `.git` 后缀，代码要 `owner/repo` 格式）。知识库 KB_CSV_URL 仍指向 `sunweave-kb`，两仓库分离更干净。
- 公开 API 查该仓库返回 "Not Found Project"（私有或不存在均如此），无法从外部确认；待用户确认：仓库已创建、设为私有、令牌有写权限、默认分支是 master 还是 main（默认写 master，若是 main 需改 GITEE_LOG_BRANCH）。
